<Window x:Class="NotesApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NotesApp - 自動翻訳対応" Height="600" Width="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ノート一覧 -->
        <StackPanel Grid.Column="0" Background="#fafafa">
            <TextBox x:Name="SearchBox" Margin="8" PlaceholderText="検索..." />
            <ListBox x:Name="NotesList" Margin="8" DisplayMemberPath="Title" />
            <Button x:Name="NewBtn" Content="＋ 新規ノート" Margin="8"/>
        </StackPanel>

        <!-- エディタ + 翻訳 -->
        <DockPanel Grid.Column="1" Background="White">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" HorizontalAlignment="Right">
                <ComboBox x:Name="TargetLangBox" Width="120" Margin="8">
                    <ComboBoxItem Content="英語 (en)" Tag="en"/>
                    <ComboBoxItem Content="日本語 (ja)" Tag="ja"/>
                    <ComboBoxItem Content="韓国語 (ko)" Tag="ko"/>
                    <ComboBoxItem Content="中国語 (zh)" Tag="zh"/>
                </ComboBox>
                <Button x:Name="TranslateBtn" Content="🌐 自動翻訳" Margin="8"/>
                <Button x:Name="SaveBtn" Content="💾 保存" Margin="8"/>
            </StackPanel>

            <TextBox x:Name="TitleBox" FontSize="18" FontWeight="Bold" Margin="8" PlaceholderText="タイトルを入力"/>
            <TextBox x:Name="BodyBox" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                     Margin="8" TextWrapping="Wrap" FontSize="14"/>
        </DockPanel>
    </Grid>
</Window>
